# MoviePilot 插件 tgsearch115 运行依赖
# Docker 内手动安装：docker exec -it moviepilot pip install beautifulsoup4 p115client

# BeautifulSoup4：解析 TG 网页预览版 HTML
beautifulsoup4>=4.12

# p115client：调用 115 分享链接转存（share_receive）
# 注意：p115client 采用 0.0.9.x 版本号；0.0.9+ 需要 Python>=3.12（MoviePilot 容器为 3.12，满足）
p115client>=0.0.9

# httpx 已随 p115client 安装，无需单独列出
